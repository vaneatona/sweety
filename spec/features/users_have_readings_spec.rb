require 'rails_helper'

RSpec.feature "UsersHaveReadings", type: :feature do

  describe "users can view their readings" do
    pending "add some examples to (or delete) #{__FILE__}"
    # They can't view another's
  end

  describe "users can create readings" do
    pending "add some examples to (or delete) #{__FILE__}"
    # They can't create more than defined (4) per day
    # They can't create for another
  end

  describe "users can edit readings" do
    pending "add some examples to (or delete) #{__FILE__}"
    # They can't edit another's
  end

  describe "users can delete readings" do
    pending "add some examples to (or delete) #{__FILE__}"
    # They can't delete another's
  end


end


RSpec.feature "UsersRunReports", type: :feature do

  describe "users can run daily reports against readings" do
    pending "add some examples to (or delete) #{__FILE__}"
    # They can't view another's
  end

  describe "users can run monthly reports against readings" do
    pending "add some examples to (or delete) #{__FILE__}"
    # They can't view another's
  end

  describe "users can run monthToDate reports against readings" do
    pending "add some examples to (or delete) #{__FILE__}"
    # They can't view another's
  end

end